<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
          "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
          "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
  <session-factory>

    <property name="hibernate.cache.provider_class">org.hibernate.cache.HashtableCacheProvider</property>
    
    <!-- Automatically create and drop schema when starting/closing application (begin) ===-->
    <property name="hibernate.hbm2ddl.auto">update</property>
    <!-- ==== Automatically create and drop schema when starting/closing application (end) -->
    
    <!-- DataSource properties (begin) ===-->
    <!-- FIXME: make this setable as it is in slide -->
    <property name="hibernate.connection.datasource">java:comp/env/jdbc/DefaultDS</property>
    <!-- ==== DataSource properties (end) -->
    
    <!-- Enable Hibernate's automatic session context management -->
    <property name="hibernate.current_session_context_class">thread</property>
    
    <!-- ###################### -->
    <!-- # idega jbpm related mapping files # -->
    <!-- ###################### -->
    <!-- TODO: those should be dynamically collected from modules configuration files. e.g. idega.hibernate.hbm.xml -->
    <mapping class="com.idega.jbpm.data.ViewTaskBind" />
    <mapping class="com.idega.jbpm.data.ActorTaskBind" />
    <mapping class="com.idega.jbpm.data.CasesJbpmBind" />
    
  </session-factory>
</hibernate-configuration>
